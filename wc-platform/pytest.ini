[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --tb=short
markers =
    render: builds a real PDF (slow, needs LibreOffice)
    realapi: makes a real Anthropic API call (costs money, needs ANTHROPIC_API_KEY)
